---
server.http2.enabled: true
spring:
  main.banner-mode: "off"
  profiles:
    active: local

logging:
  level:
    org.springframework.web.servlet: INFO

app:
  appName: "spring-kotlin-mongodb"
---
